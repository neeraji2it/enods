<%= form_for @review, :url => product_review_product_path(@product), :method => 'POST' do |f| %>
  <%= render :partial => '/layouts/error_messages', :locals => {:resource => @review} %>
  <table style="position: relative; line-height: 40px;">
    <tr>
      <td valign="top"><%= f.label :rating, "Rating#{mark}".html_safe %></td>
      <td style="float: left;margin-top: 5px;">
        <span class="rating">
          <input type="radio" id="star5" name="review[rating]" value="5" /><label for="star5">5 stars</label>
          <input type="radio" id="star4" name="review[rating]" value="4" /><label for="star4">4 stars</label>
          <input type="radio" id="star3" name="review[rating]" value="3" /><label for="star3">3 stars</label>
          <input type="radio" id="star2" name="review[rating]" value="2" /><label for="star2">2 stars</label>
          <input type="radio" id="star1" name="review[rating]" value="1" /><label for="star1">1 star</label>
        </span>
      </td>
    </tr>
    <tr>
      <td valign="top"><%= f.label :title, "Title#{mark}".html_safe %></td>
      <td><%= f.text_field :title %></td>
    </tr>
    <tr>
      <td valign="top"><%= f.label :name, "Name#{mark}".html_safe %></td>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr>
      <td valign="top"><%= f.label :message, "Message#{mark}".html_safe %></td>
      <td><%= f.text_area :message, :cols => 20, :rows => 3 %></td>
    </tr>
    <tr>
      <td></td>
      <td><%= f.submit "Review" %></td>
    </tr>
  </table>
<% end %>
<style>
  .rating:not(:checked) > input {
    position:absolute;
    top:-9999px;
    clip:rect(0,0,0,0);
  }

  .rating:not(:checked) > label {
    float:right;
    width:0.7em;
    padding:0 .1em;
    overflow:hidden;
    white-space:nowrap;
    cursor:pointer;
    font-size:200%;
    line-height:1.2;
    color: windowtext;
  }

  .rating:not(:checked) > label:before {
    content: '★';
  }

  .rating > input:checked ~ label {
    color: #f70;
  }

  .rating:not(:checked) > label:hover,
  .rating:not(:checked) > label:hover ~ label {
    color: gold;
  }

  .rating > input:checked + label:hover,
  .rating > input:checked + label:hover ~ label,
  .rating > input:checked ~ label:hover,
  .rating > input:checked ~ label:hover ~ label,
  .rating > label:hover ~ input:checked ~ label {
    color: #ea0;
  }

  .rating > label:active {
    position:relative;
  }
</style>