<h1 class="icon-profile"><em class="icon-plus"><%= (user_signed_in? and current_user.role == 'admin') ? (link_to "New Banner", new_banner_image_path) : "" %></em><span>Banner Management</span></h1>
<div class="tablebox table-categoryManagement">
  <div class="row">
    <div class="column">ID</div>
    <div class="column">Banner File Name</div>
    <div class="column">Options</div>
  </div>
  <% @banner_images.each_with_index do |category, index| %>
    <div class="row">
      <div class="column"><%= index+1 %></div>
      <div class="column"><%= image_tag(category.image(:original), :width => 100) %></div>
      <div class="column last">
        <%= link_to image_tag("/assets/admin-images/icon-edit.png"), edit_banner_image_path(category)  %>
        <%= link_to image_tag("/assets/admin-images/icon-close.png"), {:controller => 'banner_images', :action => 'destroy', :id => category.id}, :method => :delete, :confirm => "Are you Sure" %>
      </div>
    </div>
  <% end %>
</div>